@model IEnumerable<CRMS.Models.Customization.CommunicationMethod>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Customization/z_CustomizationLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CommunicationMethodName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CommunicationMethodName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CommunicationMethod_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CommunicationMethod_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CommunicationMethod_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
